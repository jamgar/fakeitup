<div class="flashes">
  <% flash.each do |type, message| %>
    <%= content_tag(:div, message, class: "#{type} px-4 py-2 rounded",
                    data: { controller: "flash" }) %>
  <% end %>
</div>
